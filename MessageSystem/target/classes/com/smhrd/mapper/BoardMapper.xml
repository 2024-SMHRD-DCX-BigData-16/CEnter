<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.mapper.BoardMapper">

	<insert id="insertBoard" parameterType="com.smhrd.entity.Board">
		insert into board(title, writer, filename, content) values(#{title}, #{writer}, #{filename}, #{content})
	</insert>
	
	<select id="boardlist" resultType="com.smhrd.entity.Board">
		select * from board
	</select>

		<!-- 게시글 상세 내역 출력 idx -> 해당 게시글의 내용을 가지고 와야한다! -->
		<select id="boardContent" parameterType="int" resultType="com.smhrd.entity.Board">
			select * from board where idx=#{idx}
		
		</select>
		
		
</mapper>